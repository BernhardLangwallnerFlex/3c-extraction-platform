{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Vetcostcheck extraction output",
  "description": "Structured invoice/receipt/prescription data extracted from a single veterinary sub-document. Translated from the JSON-Ziel-Schema embedded in products/vetcostcheck/extract_prompt.py — this file is documentation and a future validation target; it is not currently enforced against the LLM output.",
  "type": "object",
  "properties": {
    "returncode": {"type": "integer", "enum": [100, 200, 300], "description": "Klassifizierung des Subdokuments: 100=Beleg (Rechnung/Angebot), 200=kein Beleg, 300=nicht lesbar. Always present."},
    "returncodeReasons": {"type": "array", "items": {"type": "string"}, "description": "German reasons for a 200 or 300. Empty for 100."},
    "qualityFlags": {"type": "array", "items": {"type": "string", "enum": ["VISION_DROPPED", "SINGLE_ENGINE_OCR"]}, "description": "Quality caveats for this subdocument. Always present, empty when nothing degraded. VISION_DROPPED = extracted from OCR text only because the content filter rejected the page images; SINGLE_ENGINE_OCR = one OCR engine failed. Separate from returncode by design."},
    "type": {"type": ["string", "null"], "enum": ["invoice", "receipt", "prescription", null]},
    "currency": {"type": ["string", "null"], "description": "ISO 4217 code, e.g. EUR"},
    "number": {"type": ["string", "null"]},
    "issuedAt": {"type": ["string", "null"], "description": "YYYY-MM-DD"},
    "serviceDates": {"type": "array", "items": {"type": "string"}},
    "sender": {
      "type": "object",
      "properties": {
        "practiceName": {"type": ["string", "null"]},
        "address": {"type": ["string", "null"]},
        "postcode": {"type": ["string", "null"]},
        "city": {"type": ["string", "null"]},
        "country": {"type": ["string", "null"]},
        "contactPhone": {"type": ["string", "null"]},
        "contactMail": {"type": ["string", "null"]},
        "vatId": {"type": ["string", "null"]},
        "iban": {"type": ["string", "null"]},
        "bic": {"type": ["string", "null"]}
      }
    },
    "clinicians": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {"type": ["string", "null"]},
          "title": {"type": ["string", "null"]}
        }
      }
    },
    "payment": {
      "type": "object",
      "properties": {
        "iban": {"type": ["string", "null"]},
        "bic": {"type": ["string", "null"]},
        "bankName": {"type": ["string", "null"]},
        "dueDate": {"type": ["string", "null"], "description": "YYYY-MM-DD"}
      }
    },
    "recipient": {
      "type": "object",
      "properties": {
        "companyName": {"type": ["string", "null"]},
        "contactFirstname": {"type": ["string", "null"]},
        "contactName": {"type": ["string", "null"]},
        "street": {"type": ["string", "null"]},
        "postcode": {"type": ["string", "null"]},
        "city": {"type": ["string", "null"]},
        "country": {"type": ["string", "null"]},
        "contactPhone": {"type": ["string", "null"]},
        "contactMail": {"type": ["string", "null"]}
      }
    },
    "animals": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {"type": ["string", "null"]},
          "species": {"type": ["string", "null"]},
          "breed": {"type": ["string", "null"]}
        }
      }
    },
    "diagnoses": {"type": "array", "items": {"type": "string"}},
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {"type": ["string", "null"]},
          "got": {
            "type": "object",
            "properties": {
              "code": {"type": ["string", "null"]},
              "multiplier": {"type": ["number", "null"]},
              "raw": {"type": ["string", "null"]}
            }
          },
          "animal": {
            "type": "object",
            "properties": {
              "name": {"type": ["string", "null"]},
              "species": {"type": ["string", "null"]}
            }
          },
          "qty": {"type": ["number", "null"]},
          "unit": {"type": ["string", "null"]},
          "unitPriceNet": {"type": ["number", "null"]},
          "lineTotalNet": {"type": ["number", "null"]},
          "serviceDate": {"type": ["string", "null"], "description": "YYYY-MM-DD"},
          "source": {
            "type": "object",
            "properties": {
              "snippet": {"type": "string"}
            }
          }
        }
      }
    },
    "totals": {
      "type": "object",
      "properties": {
        "net": {"type": ["number", "null"]},
        "tax": {
          "type": "object",
          "properties": {
            "rate": {"type": ["number", "null"]},
            "amount": {"type": ["number", "null"]}
          }
        },
        "gross": {"type": ["number", "null"]},
        "discount": {"type": ["number", "null"]}
      }
    },
    "warnings": {"type": "array", "items": {"type": "string"}}
  }
}
